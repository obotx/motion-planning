# Copy this file to .env in the same directory if you need to customize
# Docker Compose settings (for example, a non-default DISPLAY value or
# a UID/GID that does not match the container's default user).
#
# Typical Linux values are shown below.

DISPLAY=:0
USER_UID=1000
USER_GID=1000
